# SpecKit Iterate — 2026-03-29

## Features Reviewed

- TOUR-016: Mobile bottom sheet + journey state
- TOUR-017: Tour start + completion screens
- TOUR-018: Extended nav modes + tour-level nav_mode YAML

## Product Spec Changes

- **Persona updated**: Tour Visitor now reflects 85% mobile / 10% tablet / 5% desktop split; desktop noted as primarily virtual visitors
- **In-scope feature list updated**: bottom sheet layout, journey state machine, four nav modes, tour start/completion screens, extended YAML fields all reflected in v1.0 in-scope section
- **v1.1 milestone added** to release milestones
- **YAML data format updated** to describe new fields (`tour.duration`, `tour.nav_mode`, extended `leg_to_next.mode`)

## System Architecture Changes

No changes to `speckit.plan` required for TOUR-017 or TOUR-018. TOUR-016 Task 8 will update the module decomposition in `speckit.plan` to add `src/journey/` and `src/layout/`.

## Constitution Amendment Required — Phil Sign-Off Needed

**Constitution Section II currently reads:**
> "On small screens the map takes the upper ~50% of viewport with the stop card scrolling below."

This directly contradicts TOUR-016's bottom sheet model. Proposed amendment:
> "On small screens the map fills the full viewport; stop content is presented as a draggable bottom sheet above it. On wider screens the layout may place the map and stop card side-by-side."

**This requires explicit Phil approval before implementation of TOUR-016 begins.**

## Backlog Changes

- TOUR-016, TOUR-017, TOUR-018 added as v1.1 milestone tickets with specs, plans, tasks
- TOUR-019 (WCAG audit + integration guide update for v1.1) added as post-implementation task
- All three backlog future items (GPS proximity, battery preservation) remain as-is

## Cross-Feature Findings

1. **Implementation sequence**: TOUR-016 must complete before TOUR-017 and TOUR-018 begin implementation. TOUR-017 and TOUR-018 can be implemented in parallel.

2. **Final stop breadcrumb gap** (surfaced in TOUR-017 analysis): `NavController.next()` must call `breadcrumb.markVisited()` before transitioning to `tour_complete`. Add to TOUR-016 Task 7.

3. **Saved Waze preference + non-drive mode** (TOUR-018): handled in spec and plan. No cross-feature conflict.

4. **TOUR-017 `tour.duration` field** and **TOUR-018 `tour.nav_mode` field** both modify `TourMeta` in `types.ts` — merge conflict risk if implemented on parallel branches. Note: ensure TOUR-016 Task 8 does not touch `TourMeta`; TOUR-017 Task 1 and TOUR-018 Task 1 add different fields and should merge cleanly.

5. **Demo tour.yaml** needs updating after TOUR-017 and TOUR-018 to demonstrate new fields. Covered in TOUR-018 Task 5 and TOUR-019.

## Open Questions for Phil

1. **Constitution amendment** (Section II mobile layout) — ✅ approved 2026-03-29
2. **Container positioning** — ✅ resolved: use `position: absolute` (not `fixed`) relative to container; integrator sets container height. Primary deployment (ennistidytowns.ie) is a dedicated tour page; host nav may auto-hide. Integration guide to document `height` requirement.
3. **Implementation order** — ✅ confirmed: TOUR-016 first, then TOUR-017 + TOUR-018 in parallel
